# My Project Template

This repository is a boilerplate configured with standard ignore files:
- `.gitignore` (Standard git exclusions)
- `.copilotignore` (Excludes sensitive context from GitHub Copilot)
- `.aignore` (Excludes context for Gemini, Cursor, and other AI agents)